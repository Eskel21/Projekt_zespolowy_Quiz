@using Microsoft.AspNetCore.Identity
@inject SignInManager<ApplicationUser> SignInManager
@inject UserManager<ApplicationUser> UserManager

<ul class="button-nav">
    @if (SignInManager.IsSignedIn(User))
    {
       
        
        <ul class="nav-item">
            <form class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Page("/", new { area = "" })" method="post">
                <button type="submit" class="nav-link btn btn-link text-dark">Logout</button>
            </form>
        </ul>
        
    }
    else
    {
        <ul class="nav-item">
            <a class="button-link nav-link text-dark" asp-area="Identity" asp-page="/Account/Login">
                <span>Login</span>
                <img class="log-reg-button-start" src="~/fi_arrow-right.png" />
            </a>
        </ul>
        <ul class="nav-item">
            <a class="button-link nav-link text-dark" asp-area="Identity" asp-page="/Account/Register">
                <span>Register</span>
               <img class="log-reg-button-start" src="~/fi_arrow-right.png" />
            </a>
        </ul>
    }
</ul>