﻿@using Projekt_Quizy
@inject SignInManager<IdentityUser> SignInManager
@inject SignInManager<ApplicationUser> SignInManager
@{
    var hasExternalLogins = (await SignInManager.GetExternalAuthenticationSchemesAsync()).Any();
}
<ul class="nav nav-pills flex-column">
    <li class="nav-item"><a class="account-link @ManageNavPages.IndexNavClass(ViewContext)" id="profile" asp-page="/Profile">Profile</a></li>
    <li class="nav-item"><a class="account-link @ManageNavPages.EmailNavClass(ViewContext)" id="email" asp-page="./Email">Email</a></li>
    <li class="nav-item"><a class="account-link @ManageNavPages.ChangePasswordNavClass(ViewContext)" id="change-password" asp-page="./ChangePassword">Password</a></li>
    @if (hasExternalLogins)
    {
        <li id="external-logins" class="account-link"><a id="external-login" class="nav-link @ManageNavPages.ExternalLoginsNavClass(ViewContext)" asp-page="./ExternalLogins">External logins</a></li>
    }
    <li class="nav-item">
        <a class="account-link" asp-page="./EditProfile">Name/surname</a>
    </li>
</ul>